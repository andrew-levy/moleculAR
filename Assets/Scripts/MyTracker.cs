using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MyTracker : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//DefaultTrackableEventHandler.getStatus ();
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
